language: python
python:
  - "2.7"
# command to install dependencies
install:
  - "pip install requests"
  - "git clone https://github.com/bollu/ovpl-buildbot.git"
  - "ls"
  - "ls ovpl-buildbot"
  - "cp ovpl-buildbot/client-travis.py ."
    
# command to run tests
script:
  -  "python travis.py"
